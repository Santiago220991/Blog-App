<!DOCTYPE html>
<html>
  <head>
  </head>

  <body>
  <p class="users_title">"Here is a list of users"</p>
  <% @users.each do |user|%>
    <div>
     <img src="<% user.photo%>" alt="user photo" > <p>Name:  <%=user.name%> Number of posts:  <%=user.posts_counter%></h1>
    </div>
  <% end %>
  </body>
</html>
